services:
  drivewealth-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: drivewealth-configflow:latest
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=local
    restart: unless-stopped
